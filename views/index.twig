<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Liste des villes</title>
</head>
<body>
    <h3>Liste des villes</h3>
    <form id="searchForm" action="/search" method="GET">
        <label for="ville">Nom de la ville :</label>
        <input type="text" id="ville" name="ville" placeholder="Rechercher une ville...">
        <button type="submit">Rechercher</button>
    </form>
    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Longitude</th>
                <th>Latitude</th>
            </tr>
        </thead>
        <tbody id="citiesTableBody">
            {% for city in cities %}
            <tr>
                <td>{{ city.name }}</td>
                <td>{{ city.long }}</td>
                <td>{{ city.lat }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <script src="/script.js"></script> <!-- Ajoutez cette ligne pour inclure le fichier JavaScript -->
</body>
</html>